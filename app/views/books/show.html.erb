<div>
  <%= flash[:notice] %>
</div>

  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>

<div class="container px-5 px-sm-0">
  <div class="row">
   <div class="col-md-3">
  <%= render 'users/info', user: @user %>
  <%= render 'list', book: @book %>
  </div>


<div class="col-md-8 offset-md-1">
  <h2>Book detail</h2>
  <table class="table">
　　<tbody>
　　  <tr>
    <td><% if @book.image.attached? %>
      <th><%= image_tag @books.image, size: "100x100" %><br>
      <%=link_to @book.user.name, books_path %></th>
    <% else %>
      <th><%= image_tag 'nimage', size: "100x100" %><br>
      <%=link_to @book.user.name, books_path %></th>
    <% end %>
    </td>
    <td><%=link_to @book.title, book_path %></td>
    <td><%= @book.body%></td>
    <td><%= link_to "Edit", "/books/#{@book.id}/edit" %></td>
    <td><%= link_to "Destroy", "/books/#{@book.id}", method: :delete, "data-confirm" => "Are you sure？" %></td>
　　　</tr></tbody>
　　</table>
　</div>
　</div>
</div>