<div>
  <%= flash[:notice] %>
</div>

  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>

<div class="container px-5 px-sm-0">
  <div class="row">
   <div class="col-md-3">
  <%= render 'users/info', user: @user %>
  <%= render 'list', book: @book %>
  </div>


<div class="col-md-8 offset-md-1">
  <h2>Book detail</h2>
  <table class="table">
　　<tbody><tr>
    <td>
    <th><%= image_tag @book.user.get_profile_image(100,100) %><br>
      <%=link_to @book.user.name, books_path(@user) %></th>
    </td>
    <td><%=link_to @book.title, book_path %></td>
    <td><%= @book.body%></td>
    <td><%= link_to "Edit", "/books/#{@book.id}/edit", class:"btn btn-success"%></td>
    <td><%= link_to "Destroy", "/books/#{@book.id}", method: :delete, class:"btn btn-danger", "data-confirm" => "本当に消しますか？"%>
    </td>
　　　</tr></tbody>
　　</table>
　</div>
　</div>
</div>